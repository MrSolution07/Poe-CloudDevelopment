@model EventEaseApp.Models.Booking

@{
    ViewData["Title"] = "Booking Details";
}

<h2>Booking Details</h2>
<hr />

<div class="row">
    <div class="col-md-6">
        <dl class="row">
            <dt class="col-sm-4">@Html.DisplayNameFor(m => m.BookingId)</dt>
            <dd class="col-sm-8">@Model.BookingId</dd>

            <dt class="col-sm-4">Event</dt>
            <dd class="col-sm-8">@Model.Event?.EventName</dd>

            <dt class="col-sm-4">Venue</dt>
            <dd class="col-sm-8">@Model.Venue?.VenueName</dd>

            <dt class="col-sm-4">@Html.DisplayNameFor(m => m.BookingDate)</dt>
            <dd class="col-sm-8">@Model.BookingDate.ToString("dd MMM yyyy")</dd>
        </dl>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.BookingId" class="btn btn-warning">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
